version: '3.8'

services:
  meta-prompt-webapp:
    build: .
    ports:
      - "8080:5000"
    environment:
      - FLASK_ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
    user: "${DOCKER_USER_ID:-1000}:1000"
    env_file:
      - .env